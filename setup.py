import setuptools

with open("README.md", "r", encoding="utf-8") as fh:
    long_description = fh.read()

setuptools.setup(
    name='bits',
    version='0.1.0',
    author='@fkadibs',
    packages=['bits']
)